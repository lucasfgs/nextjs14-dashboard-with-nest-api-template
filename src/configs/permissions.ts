export enum EPermission {
  USERS = "users",
  ROLES = "roles",
  SETTINGS = "settings",
}

export enum EPermissionType {
  CREATE = "create",
  READ = "read",
  UPDATE = "update",
  DELETE = "delete",
}
